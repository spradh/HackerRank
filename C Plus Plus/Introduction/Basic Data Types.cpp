#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    // Complete the code.
    int in;
    long lo;
    char ch;
    float fl;
    double dou;
    scanf("%d %ld %c %f %lf",&in, &lo, &ch, &fl, &dou);
    printf("%d\n%ld\n%c\n%f\n%lf",in, lo, ch, fl, dou);
    return 0;
}
